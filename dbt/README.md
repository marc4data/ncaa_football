# dbt — project skeleton

This folder will contain the `dbt` project for transforms: `models/`, `macros/`, `tests/`, and `dbt_project.yml`.

For now this is a placeholder. When ready, run `dbt init` here or copy the curated `dbt_project.yml` into this folder.
